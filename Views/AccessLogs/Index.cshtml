@model IEnumerable<SMSGateway.Models.AccessLogs>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LogDateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LoggedFrom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Saf.Password)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LogDateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LoggedFrom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Saf.Password)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LogId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LogId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LogId })
        </td>
    </tr>
}
</table>
